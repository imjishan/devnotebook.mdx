# Palette's Journal

## 2025-02-19 - Admin Login Accessibility & UX
**Learning:** Simple admin logins often neglect basic accessibility (label/input association) and error handling (blocking alerts).
**Action:** Always check `htmlFor` and `id` on inputs. Replace `alert()` with inline error states and `aria-invalid`.
