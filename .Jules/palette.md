## 2025-05-23 - Alert Usage Pattern
**Learning:** This app frequently uses `alert()` for error messages (e.g., login failure, save confirmation), which is an accessibility and UX anti-pattern.
**Action:** Replace `alert()` with inline validation messages or toast notifications for future enhancements.
